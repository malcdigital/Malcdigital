# Core - required
pandas>=2.0
numpy>=1.24
requests>=2.31  # yfinance requires >=2.31; a lower floor is unsatisfiable

# Live market data (free, no API key). Without it the desk falls back to
# Stooq, and then to clearly-labelled generated data.
yfinance>=0.2.40

# Optional: trade against a real Alpaca paper account instead of the simulator
# alpaca-py>=0.20

# Development
# pytest>=7.0
